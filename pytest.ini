[pytest]
asyncio_mode = auto
markers =
    asyncio: mark test as async
    avb: AVB network audio tests (skipped when hardware unavailable)
    avdecc_mock: AVDECC mock-harness integration tests
    performance: performance/timing tests
    integration: end-to-end integration tests
