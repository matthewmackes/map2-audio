================================================================================
MAP2 AUDIO PLATFORM - LATENCY OPTIMIZATION IMPLEMENTATION
Date: 2026-02-08
Status: COMPLETE - BUILD IN PROGRESS
================================================================================

ALL 5 RECOMMENDED OPTIONS HAVE BEEN APPLIED:

✅ OPTION 1: Quantum Reduction (256 → 128 samples)
   Savings: 5.3ms per round-trip
   Files Modified:
   - juce-engine/Source/Common.h (DEFAULT_BUFFER_SIZE = 128)
   - systemd/map2-backend.service (pw-metadata force-quantum 128)
   - packaging/systemd/map2-backend.service
   - ~/.config/pipewire/pipewire.conf.d/10-low-latency.conf (quantum = 128)
   - /etc/systemd/system/map2-backend.service.d/override.conf

✅ OPTION 2: Convolution ZeroLatency Mode
   Savings: 1–2 buffer periods (2.67–5.3ms)
   Files Modified:
   - juce-engine/Source/Map2AudioEngine.cpp
     • cabinetProcessor_.setMode(ConvolutionProcessor::Mode::ZeroLatency)
     • reverbProcessor_.setMode(ConvolutionProcessor::Mode::ZeroLatency)

✅ OPTION 3: Metering Off Audio Thread
   Savings: 0.5–1.5ms CPU headroom (indirect, enables smaller quantum)
   Files Modified:
   - juce-engine/Source/Map2AudioEngine.h
     • Added meteringThread_, meteringQueue_, meteringQueueCV_
     • Added pushMeteringData(), meteringThreadFunc()
   - juce-engine/Source/Map2AudioEngine.cpp
     • Metering thread initialization/shutdown
     • Moved spectrumAnalyzer_, lufsMeter_, phaseCorrelation_, 
       masterVuMeter_ off audio callback
     • Lock-free FIFO for metering data (drops frames safely if full)

✅ OPTION 5: PIPEWIRE_LATENCY Environment Hint
   Savings: Up to 2.67ms
   Files Modified:
   - systemd/map2-backend.service (Environment="PIPEWIRE_LATENCY=128/48000")
   - packaging/systemd/map2-backend.service
   - /etc/systemd/system/map2-backend.service.d/override.conf

✅ OPTION 9: ALSA nperiods=2 (Triple-Buffering Reduction)
   Savings: 2.67ms (1 buffer period)
   Files Modified:
   - ~/.config/pipewire/pipewire.conf.d/10-low-latency.conf
     • api.alsa.period-num = 2
     • api.alsa.headroom = 0

ADDITIONAL FIXES:
✅ CMakeLists.txt: Added PipeWire JACK library path detection
   - Resolves linker errors with pipewire-jack-audio-connection-kit

================================================================================
EXPECTED LATENCY REDUCTION
================================================================================

Before:  10.6ms round-trip (256 sample quantum)
After:   3–4ms round-trip (128 sample quantum + optimizations)
Savings: ~7–8ms (62–75% reduction)

Breakdown:
  PipeWire graph (256→128):     5.33ms → 2.67ms  (−2.67ms)
  Driver latency (256→128):     5.33ms → 2.67ms  (−2.67ms)
  Convolution (LowLatency→Zero): 2.67ms → 0ms    (−2.67ms)
  ──────────────────────────────────────────────────────────
  Total round-trip:            10.6ms → 3.3ms   (−7.3ms)

Plus CPU headroom from metering off-thread allows even smaller quantum if needed.

================================================================================
BUILD STATUS
================================================================================

Current: Single-threaded rebuild in progress
Status: Compiling source files (~58% complete as of last check)
ETA: 30–60 minutes to completion

Expected artifact:
  /home/mm/map2-audio/juce-engine/build/map2_audio_engine.cpython-314-x86_64-linux-gnu.so

Build command:
  cd /home/mm/map2-audio/juce-engine/build && make -j1

================================================================================
DEPLOYMENT STEPS (After Build Complete)
================================================================================

1. Verify module exists:
   ls -lh /home/mm/map2-audio/juce-engine/build/map2_audio_engine*.so

2. Reload systemd:
   sudo systemctl daemon-reload
   systemctl --user daemon-reload

3. Restart PipeWire to apply new quantum:
   systemctl --user restart pipewire.service wireplumber.service

4. Restart MAP2 backend:
   sudo systemctl restart map2-backend

5. Verify configuration:
   pw-metadata -n settings 2>/dev/null | grep "clock.quantum"
   # Should show: update: id:0 key:'clock.quantum' value:'128'

6. Monitor for XRuns:
   pw-top -b | head -20
   # Check for XRun column if module is active

================================================================================
TESTING RECOMMENDATIONS
================================================================================

1. Low-latency plugin test:
   - Load a simple reverb IR (cabinet)
   - Verify zero-latency operation with test signal
   - Check latency report in LUFS meter

2. CPU load test:
   - Load multiple plugins at 128 quantum
   - Monitor CPU usage: ps aux | grep uvicorn
   - Monitor XRuns: Check system logs

3. Audio quality test:
   - Compare frequency response at 128 vs 256 quantum
   - Verify no artifacts from convolution ZeroLatency mode
   - Test with high-frequency content (>15kHz)

4. Fallback procedure:
   If XRuns occur at 128 quantum:
   - Temporarily revert: pw-metadata -n settings 0 clock.force-quantum 256
   - Verify stability
   - Then disable metering thread: Edit Map2AudioEngine.cpp line ~131
     • Comment out: meteringThread_ = std::thread([this]() { ... });

================================================================================
REVERTING (If Needed)
================================================================================

All changes are reversible with configuration edits:

1. Revert quantum to 256:
   - Edit ~/.config/pipewire/pipewire.conf.d/10-low-latency.conf
     • default.clock.quantum = 256
   - Edit /etc/systemd/system/map2-backend.service.d/override.conf
     • ExecStartPre=/usr/bin/pw-metadata -n settings 0 clock.force-quantum 256

2. Revert ALSA nperiods:
   - Edit ~/.config/pipewire/pipewire.conf.d/10-low-latency.conf
     • api.alsa.period-num = 3 (or remove line for default)

3. Rebuild module (recompiles all changes):
   cd /home/mm/map2-audio/juce-engine/build && make clean && make -j1

================================================================================
DOCUMENTATION
================================================================================

For complete details, see:
- LATENCY_OPTIMIZATIONS_APPLIED.md (comprehensive implementation guide)
- CRITICAL_FIXES_COMPLETE.md (earlier fixes reference)
- IMPLEMENTATION_CHECKLIST.md (task tracking)

================================================================================
